{{ 'section-image-banner.css' | asset_url | stylesheet_tag }}

{%- if section.settings.image_height == 'adapt' and section.settings.image != blank -%}
  {%- style -%}
    @media screen and (max-width: 749px) {
      #Banner-{{ section.id }}::before,
      #Banner-{{ section.id }} .banner__media::before,
      #Banner-{{ section.id }}:not(.banner--mobile-bottom) .banner__content::before {
        padding-bottom: {{ 1 | divided_by: section.settings.image.aspect_ratio | times: 100 }}%;
        content: '';
        display: block;
      }
    }

    @media screen and (min-width: 750px) {
      #Banner-{{ section.id }}::before,
      #Banner-{{ section.id }} .banner__media::before {
        padding-bottom: {{ 1 | divided_by: section.settings.image.aspect_ratio | times: 100 }}%;
        content: '';
        display: block;
      }
    }
  {%- endstyle -%}
{%- endif -%}

{%- style -%}
  #Banner-{{ section.id }}::after {
    opacity: {{ section.settings.image_overlay_opacity | divided_by: 100.0 }};
  }

  @media screen and (min-width: 750px) {
    #Banner-{{ section.id }}:not(.banner--adapt) {
      min-height: {{ section.settings.image_height_desktop_custom }}rem;
    }
  }

  {%- if section.settings.nav_hero_gap != blank -%}
  #Banner-{{ section.id }} {
    margin-top: calc({{ section.settings.nav_hero_gap }}px - 18px);
  }
  {%- endif -%}

  #Banner-{{ section.id }} .banner__media > img,
  #Banner-{{ section.id }} .banner__media > video,
  #Banner-{{ section.id }} .banner__media .hero-video {
    object-position: {{ section.settings.hero_image_position_x }}% {{ section.settings.hero_image_position_y }}%;
  }

  /* Mobile : recentrage horizontal forcé (50%) pour éviter une image
     calée sur son bord gauche. Desktop conserve hero_image_position_x. */
  @media screen and (max-width: 749px) {
    #Banner-{{ section.id }} .banner__media > img,
    #Banner-{{ section.id }} .banner__media > video,
    #Banner-{{ section.id }} .banner__media .hero-video {
      object-position: 50% {{ section.settings.hero_image_position_y }}% !important;
    }
  }

  {%- liquid
    assign hero_title_size = section.settings.hero_title_size | default: 64
    assign hero_title_min = hero_title_size | times: 60 | divided_by: 100
    if hero_title_min < 32
      assign hero_title_min = 32
    endif
    assign hero_title_mobile = hero_title_size | times: 70 | divided_by: 100
    if hero_title_mobile < 32
      assign hero_title_mobile = 32
    endif
  -%}
  #Banner-{{ section.id }} .banner__heading,
  #Banner-{{ section.id }} .banner__heading.inline-richtext {
    font-size: clamp({{ hero_title_min }}px, 5vw, {{ hero_title_size }}px) !important;
  }
  @media screen and (max-width: 749px) {
    #Banner-{{ section.id }} .banner__heading,
    #Banner-{{ section.id }} .banner__heading.inline-richtext {
      font-size: clamp(32px, 7vw, {{ hero_title_mobile }}px) !important;
    }
  }

  {%- comment -%}
    Phase 16.58 : les styles du bouton CTA hero sont portés par la
    classe globale .mc-cta-primary (assets/maison-coeur.css). Au
    repos, hover, focus-visible et :active (force brute Phase 16.61),
    les propriétés sont rendues à partir de variables CSS.

    Phase 16.62 : les 5 settings dédiés (section image-banner) sont
    injectés en style inline sur les <a> du hero, ce qui scope les
    variables CSS UNIQUEMENT à ces éléments. Le bouton hero devient
    indépendant des settings globaux mc_cta_primary_* (Réglages du
    thème), qui continuent à piloter les autres CTA primaires
    (Notre histoire, etc.).
  {%- endcomment -%}

  {%- liquid
    assign overlay_color = section.settings.hero_overlay_color | default: '#000000'
    assign overlay_r = overlay_color | color_extract: 'red'
    assign overlay_g = overlay_color | color_extract: 'green'
    assign overlay_b = overlay_color | color_extract: 'blue'
    assign overlay_pct = section.settings.hero_overlay_opacity_pct | default: 8
    assign overlay_alpha = overlay_pct | divided_by: 100.0

    assign scroll_color = section.settings.hero_scroll_indicator_color | default: '#FFFFFF'
    assign scroll_r = scroll_color | color_extract: 'red'
    assign scroll_g = scroll_color | color_extract: 'green'
    assign scroll_b = scroll_color | color_extract: 'blue'
    assign scroll_opacity = section.settings.hero_scroll_indicator_opacity | default: 1
    assign scroll_height = section.settings.hero_scroll_indicator_height | default: 50

    assign kb_amp = section.settings.hero_kenburns_amplitude_pct | divided_by: 100.0 | plus: 1
    assign aspect_ratio = section.settings.hero_aspect_ratio | default: '16/9'
    assign max_height_vh = section.settings.hero_max_height_vh | default: 90

    # Phase 16.62 — Variables CTA hero scopées (override des :root globaux).
    # Injectées en style inline sur les <a class="mc-cta-primary"> du hero.
    # Mappage sur les noms de variables effectivement consommés par
    # .mc-cta-primary dans assets/maison-coeur.css (rgb triplets + alpha).
    assign hero_cta_rest = section.settings.hero_cta_color_rest | default: '#FFFFFF'
    assign hero_cta_rest_r = hero_cta_rest | color_extract: 'red'
    assign hero_cta_rest_g = hero_cta_rest | color_extract: 'green'
    assign hero_cta_rest_b = hero_cta_rest | color_extract: 'blue'
    assign hero_cta_rest_a = section.settings.hero_cta_opacity_rest | default: 1

    assign hero_cta_bg = section.settings.hero_cta_bg_hover | default: '#FFFFFF'
    assign hero_cta_bg_r = hero_cta_bg | color_extract: 'red'
    assign hero_cta_bg_g = hero_cta_bg | color_extract: 'green'
    assign hero_cta_bg_b = hero_cta_bg | color_extract: 'blue'

    assign hero_cta_tx = section.settings.hero_cta_color_hover | default: '#332821'
    assign hero_cta_tx_r = hero_cta_tx | color_extract: 'red'
    assign hero_cta_tx_g = hero_cta_tx | color_extract: 'green'
    assign hero_cta_tx_b = hero_cta_tx | color_extract: 'blue'

    assign hero_cta_hover_a = section.settings.hero_cta_opacity_hover | default: 1

    # Phase 16.64 — Taille et padding du bouton hero (scopés).
    # Restaure un rendu compact (font 12px, padding 32x14) altéré par
    # la Phase 16.63 qui a coupé les overrides legacy. Pilotables via
    # 3 settings dédiés.
    assign hero_cta_font = section.settings.hero_cta_font_size | default: 12
    assign hero_cta_padx = section.settings.hero_cta_padding_horizontal | default: 32
    assign hero_cta_pady = section.settings.hero_cta_padding_vertical | default: 14
  -%}

  @media screen and (min-width: 750px) {
    #Banner-{{ section.id }}:not(.banner--adapt) {
      width: 100% !important;
      aspect-ratio: {{ aspect_ratio }} !important;
      max-height: {{ max_height_vh }}vh !important;
      min-height: 0 !important;
      height: auto !important;
    }
  }

  #Banner-{{ section.id }}::after {
    background-color: rgba({{ overlay_r }}, {{ overlay_g }}, {{ overlay_b }}, {{ overlay_alpha }}) !important;
    background-color: color-mix(in srgb, {{ overlay_color }} {{ overlay_pct }}%, transparent) !important;
    background-image: none !important;
    opacity: 1 !important;
  }

  #Banner-{{ section.id }} .banner__box .banner__heading + .banner__text,
  #Banner-{{ section.id }} .banner__box > .banner__heading + .banner__text {
    margin-top: {{ section.settings.hero_spacing_title_to_subtitle }}px !important;
  }

  @media screen and (min-width: 750px) {
    #Banner-{{ section.id }} .banner__box > * + .banner__text {
      margin-top: {{ section.settings.hero_spacing_title_to_subtitle }}px !important;
    }
  }

  #Banner-{{ section.id }} .banner__box > * + .banner__buttons,
  #Banner-{{ section.id }} .banner__box .banner__text + .banner__buttons {
    margin-top: {{ section.settings.hero_spacing_subtitle_to_cta }}px !important;
  }

  {%- if section.settings.hero_kenburns_enabled -%}
    @keyframes mc-hero-kenburns-{{ section.id }} {
      0%   { transform: scale(1); }
      50%  { transform: scale({{ kb_amp }}); }
      100% { transform: scale(1); }
    }
    #Banner-{{ section.id }} .banner__media > img,
    #Banner-{{ section.id }} .banner__media > svg {
      animation: mc-hero-kenburns-{{ section.id }} {{ section.settings.hero_kenburns_duration }}s ease-in-out infinite;
      transform-origin: center center;
      will-change: transform;
    }
    @media (prefers-reduced-motion: reduce) {
      #Banner-{{ section.id }} .banner__media > img,
      #Banner-{{ section.id }} .banner__media > svg {
        animation: none !important;
        transform: none !important;
      }
    }
  {%- endif -%}

  .mc-hero-scroll-indicator {
    position: absolute;
    left: 50%;
    bottom: 2.4rem;
    transform: translateX(-50%);
    width: 1.5px;
    height: {{ scroll_height }}px;
    padding: 0;
    margin: 0;
    border: 0;
    background: transparent;
    cursor: pointer;
    z-index: 3;
    -webkit-appearance: none;
    appearance: none;
  }
  .mc-hero-scroll-indicator::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 1.5px;
    height: 100%;
    background: rgb({{ scroll_r }}, {{ scroll_g }}, {{ scroll_b }});
    opacity: {{ scroll_opacity }};
    animation: mc-hero-scroll-pulse-{{ section.id }} 2s ease-in-out infinite;
  }
  .mc-hero-scroll-indicator:focus { outline: 0; }
  .mc-hero-scroll-indicator:focus-visible {
    outline: 2px solid rgba({{ scroll_r }}, {{ scroll_g }}, {{ scroll_b }}, 0.5);
    outline-offset: 4px;
  }
  @keyframes mc-hero-scroll-pulse-{{ section.id }} {
    0%   { opacity: {{ scroll_opacity | times: 0.6 }}; transform: translateY(0); }
    50%  { opacity: {{ scroll_opacity }}; transform: translateY(9px); }
    100% { opacity: {{ scroll_opacity | times: 0.6 }}; transform: translateY(0); }
  }
  @media (prefers-reduced-motion: reduce) {
    .mc-hero-scroll-indicator::before {
      animation: none !important;
      opacity: {{ scroll_opacity }};
    }
  }
{%- endstyle -%}

{%- liquid
  assign full_width = '100vw'
  assign widths = '375, 550, 750, 1100, 1500, 1780, 2000, 3000, 3840'

  if section.settings.image_behavior == 'ambient'
    assign half_width = '60vw'
    assign full_width = '120vw'
    assign stacked_sizes = '(min-width: 750px) 60vw, 120vw'
    assign widths = '450, 660, 900, 1320, 1800, 2136, 2400, 3600, 7680'
  elsif section.settings.image_behavior == 'fixed' or section.settings.image_behavior == 'zoom-in'
    assign half_width = '100vw'
    assign stacked_sizes = '100vw'
  else
    assign half_width = '50vw'
    assign stacked_sizes = '(min-width: 750px) 50vw, 100vw'
  endif
  assign fetch_priority = 'auto'
  if section.index == 1
    assign fetch_priority = 'high'
  endif
-%}

<div
  id="Banner-{{ section.id }}"
  class="banner banner--content-align-{{ section.settings.desktop_content_alignment }} banner--content-align-mobile-{{ section.settings.mobile_content_alignment }} banner--{{ section.settings.image_height }}{% if section.settings.stack_images_on_mobile and section.settings.image != blank and section.settings.image_2 != blank %} banner--stacked{% endif %}{% if section.settings.image_height == 'adapt' and section.settings.image != blank %} banner--adapt{% endif %}{% if section.settings.show_text_below %} banner--mobile-bottom{%- endif -%}{% if section.settings.show_text_box == false %} banner--desktop-transparent{% endif %}{% if settings.animations_reveal_on_scroll %} scroll-trigger animate--fade-in{% endif %}"
>
  {%- if section.settings.image != blank -%}
    <div class="banner__media media{% if section.settings.image == blank and section.settings.image_2 == blank %} placeholder{% endif %}{% if section.settings.image_2 != blank %} banner__media-half{% endif %}{% if section.settings.image_behavior != 'none' %} animate--{{ section.settings.image_behavior }}{% endif %}{% if settings.animations_reveal_on_scroll %} scroll-trigger animate--fade-in{% endif %}">
      {%- liquid
        assign image_height = section.settings.image.width | divided_by: section.settings.image.aspect_ratio
        if section.settings.image_2 != blank
          assign image_class = 'banner__media-image-half'
        endif
        if section.settings.image_2 != blank and section.settings.stack_images_on_mobile
          assign sizes = stacked_sizes
        elsif section.settings.image_2 != blank
          assign sizes = half_width
        else
          assign sizes = full_width
        endif
      -%}
      {% if section.settings.hero_video != blank %}
        {{
          section.settings.hero_video
          | video_tag:
            autoplay: true,
            loop: true,
            muted: true,
            playsinline: true,
            controls: false,
            class: 'hero-video'
        }}
      {% else %}
        {{
          section.settings.image
          | image_url: width: 3840
          | image_tag:
            width: section.settings.image.width,
            height: image_height,
            class: image_class,
            sizes: sizes,
            widths: widths,
            fetchpriority: fetch_priority
        }}
      {% endif %}
    </div>
  {%- elsif section.settings.image_2 == blank -%}
    <div class="banner__media media{% if section.settings.image == blank and section.settings.image_2 == blank %} placeholder{% endif %}{% if section.settings.image_2 != blank %} banner__media-half{% endif %}{% if section.settings.image_behavior != 'none' %} animate--{{ section.settings.image_behavior }}{% endif %}{% if settings.animations_reveal_on_scroll %} scroll-trigger animate--fade-in{% endif %}">
      {{ 'hero-apparel-1' | placeholder_svg_tag: 'placeholder-svg' }}
    </div>
  {%- endif -%}
  {%- if section.settings.image_2 != blank -%}
    <div class="banner__media media{% if section.settings.image != blank %} banner__media-half{% endif %}{% if section.settings.image_behavior != 'none' %} animate--{{ section.settings.image_behavior }}{% endif %}{% if settings.animations_reveal_on_scroll %} scroll-trigger animate--fade-in{% endif %}">
      {%- liquid
        assign image_height_2 = section.settings.image_2.width | divided_by: section.settings.image_2.aspect_ratio
        if section.settings.image != blank
          assign image_class_2 = 'banner__media-image-half'
        endif
        if section.settings.image != blank and section.settings.stack_images_on_mobile
          assign sizes = stacked_sizes
        elsif section.settings.image_2 != blank
          assign sizes = half_width
        else
          assign sizes = full_width
        endif
      -%}
      {{
        section.settings.image_2
        | image_url: width: 3840
        | image_tag:
          width: section.settings.image_2.width,
          height: image_height_2,
          class: image_class_2,
          sizes: sizes,
          widths: widths,
          fetchpriority: fetch_priority
      }}
    </div>
  {%- endif -%}
  {%- if section.settings.hero_scroll_indicator_enabled -%}
    <button
      type="button"
      class="mc-hero-scroll-indicator"
      data-mc-hero-scroll
      data-banner-id="Banner-{{ section.id }}"
      aria-label="Faire défiler vers la section suivante"
    ></button>
  {%- endif -%}
  <div class="banner__content banner__content--{{ section.settings.desktop_content_position }} page-width{% if settings.animations_reveal_on_scroll %} scroll-trigger animate--slide-in{% endif %}">
    <div class="banner__box content-container content-container--full-width-mobile color-{{ section.settings.color_scheme }} gradient">
      {%- for block in section.blocks -%}
        {%- case block.type -%}
          {%- when 'heading' -%}
            <h2
              class="banner__heading inline-richtext {{ block.settings.heading_size }}"
              {{ block.shopify_attributes }}
            >
              {{ block.settings.heading }}
            </h2>
          {%- when 'text' -%}
            <div class="banner__text rte {{ block.settings.text_style }}" {{ block.shopify_attributes }}>
              <p>{{ block.settings.text }}</p>
            </div>
          {%- when 'buttons' -%}
            <div
              class="banner__buttons{% if block.settings.button_label_1 != blank and block.settings.button_label_2 != blank %} banner__buttons--multiple{% endif %}"
              {{ block.shopify_attributes }}
            >
              {%- if block.settings.button_label_1 != blank -%}
                <a
                  {% if block.settings.button_link_1 == blank %}
                    role="link" aria-disabled="true"
                  {% else %}
                    href="{{ block.settings.button_link_1 }}"
                  {% endif %}
                  class="button{% if block.settings.button_style_secondary_1 %} button--secondary{% else %} button--primary mc-cta-primary{% endif %}"
                  style="--mc-cta-primary-rest-rgb: {{ hero_cta_rest_r }}, {{ hero_cta_rest_g }}, {{ hero_cta_rest_b }}; --mc-cta-primary-rest-alpha: {{ hero_cta_rest_a }}; --mc-cta-primary-hover-bg-rgb: {{ hero_cta_bg_r }}, {{ hero_cta_bg_g }}, {{ hero_cta_bg_b }}; --mc-cta-primary-hover-color-rgb: {{ hero_cta_tx_r }}, {{ hero_cta_tx_g }}, {{ hero_cta_tx_b }}; --mc-cta-primary-hover-alpha: {{ hero_cta_hover_a }}; --mc-cta-primary-font-size: {{ hero_cta_font }}px; --mc-cta-primary-padding-horizontal: {{ hero_cta_padx }}px; --mc-cta-primary-padding-vertical: {{ hero_cta_pady }}px;"
                >
                  {{- block.settings.button_label_1 | escape -}}
                </a>
              {%- endif -%}
              {%- if block.settings.button_label_2 != blank -%}
                <a
                  {% if block.settings.button_link_2 == blank %}
                    role="link" aria-disabled="true"
                  {% else %}
                    href="{{ block.settings.button_link_2 }}"
                  {% endif %}
                  class="button{% if block.settings.button_style_secondary_2 %} button--secondary{% else %} button--primary mc-cta-primary{% endif %}"
                  style="--mc-cta-primary-rest-rgb: {{ hero_cta_rest_r }}, {{ hero_cta_rest_g }}, {{ hero_cta_rest_b }}; --mc-cta-primary-rest-alpha: {{ hero_cta_rest_a }}; --mc-cta-primary-hover-bg-rgb: {{ hero_cta_bg_r }}, {{ hero_cta_bg_g }}, {{ hero_cta_bg_b }}; --mc-cta-primary-hover-color-rgb: {{ hero_cta_tx_r }}, {{ hero_cta_tx_g }}, {{ hero_cta_tx_b }}; --mc-cta-primary-hover-alpha: {{ hero_cta_hover_a }}; --mc-cta-primary-font-size: {{ hero_cta_font }}px; --mc-cta-primary-padding-horizontal: {{ hero_cta_padx }}px; --mc-cta-primary-padding-vertical: {{ hero_cta_pady }}px;"
                >
                  {{- block.settings.button_label_2 | escape -}}
                </a>
              {%- endif -%}
            </div>
        {%- endcase -%}
      {%- endfor -%}
    </div>
  </div>
</div>

{%- if section.settings.hero_scroll_indicator_enabled -%}
  <script>
    (function () {
      var btn = document.querySelector('[data-mc-hero-scroll][data-banner-id="Banner-{{ section.id }}"]');
      if (!btn) return;
      btn.addEventListener('click', function () {
        var banner = document.getElementById('Banner-{{ section.id }}');
        if (!banner) return;
        var holder = banner.closest('.shopify-section') || banner;
        var next = holder.nextElementSibling;
        while (next && next.offsetHeight === 0) next = next.nextElementSibling;
        if (next && typeof next.scrollIntoView === 'function') {
          next.scrollIntoView({ behavior: 'smooth', block: 'start' });
        }
      });
    })();
  </script>
{%- endif -%}

{% schema %}
{
  "name": "t:sections.image-banner.name",
  "tag": "section",
  "class": "section",
  "disabled_on": {
    "groups": ["header", "footer"]
  },
  "settings": [
    {
      "type": "video",
      "id": "hero_video",
      "label": "Vidéo (optionnelle, remplace l'image)",
      "info": "MP4 H.264, ratio 2.4:1, 8-12s, max 5 Mo, sans son. Lecture auto, muet, boucle."
    },
    {
      "type": "image_picker",
      "id": "image",
      "label": "t:sections.image-banner.settings.image.label"
    },
    {
      "type": "image_picker",
      "id": "image_2",
      "label": "t:sections.image-banner.settings.image_2.label"
    },
    {
      "type": "range",
      "id": "image_overlay_opacity",
      "min": 0,
      "max": 100,
      "step": 10,
      "unit": "%",
      "label": "t:sections.image-banner.settings.image_overlay_opacity.label",
      "default": 0
    },
    {
      "type": "range",
      "id": "nav_hero_gap",
      "min": 0,
      "max": 80,
      "step": 4,
      "unit": "px",
      "label": "Espace entre menu et hero (px)",
      "info": "Distance verticale entre la fin de la nav éditoriale et le haut du hero.",
      "default": 8
    },
    {
      "type": "range",
      "id": "hero_image_position_x",
      "min": 0,
      "max": 100,
      "step": 5,
      "unit": "%",
      "label": "Position horizontale image (%)",
      "info": "0 = bord gauche cadré, 50 = centre, 100 = bord droit cadré.",
      "default": 50
    },
    {
      "type": "range",
      "id": "hero_image_position_y",
      "min": 0,
      "max": 100,
      "step": 5,
      "unit": "%",
      "label": "Position verticale image (%)",
      "info": "0 = haut cadré, 50 = centre, 100 = bas cadré.",
      "default": 50
    },
    {
      "type": "range",
      "id": "hero_title_size",
      "min": 32,
      "max": 96,
      "step": 2,
      "unit": "px",
      "label": "Taille du titre hero (px)",
      "info": "Taille du titre Cormorant italique. Clamp adaptatif appliqué entre mobile et desktop.",
      "default": 64
    },
    {
      "type": "header",
      "content": "Bouton CTA du hero — couleurs et opacités dédiées"
    },
    {
      "type": "paragraph",
      "content": "Ces réglages pilotent le bouton hero INDÉPENDAMMENT des autres CTA primaires du site (NOTRE HISTOIRE, etc.) qui restent pilotés globalement depuis Réglages du thème » CTA éditoriaux (home)."
    },
    {
      "type": "color",
      "id": "hero_cta_color_rest",
      "label": "Couleur du bouton (au repos)",
      "info": "Couleur du contour fin et du texte avant survol. Default blanc pour boutons sur image sombre.",
      "default": "#FFFFFF"
    },
    {
      "type": "range",
      "id": "hero_cta_opacity_rest",
      "label": "Opacité du bouton (au repos)",
      "info": "Présence du contour et du texte avant survol. 1 = pleine présence, valeurs plus basses = bouton plus discret.",
      "min": 0.2,
      "max": 1,
      "step": 0.1,
      "default": 1
    },
    {
      "type": "color",
      "id": "hero_cta_bg_hover",
      "label": "Couleur de fond au survol et au clic",
      "info": "Couleur du fond plein qui apparaît au survol et au clic. Default blanc pour rendu gallery sur image.",
      "default": "#FFFFFF"
    },
    {
      "type": "color",
      "id": "hero_cta_color_hover",
      "label": "Couleur du texte au survol et au clic",
      "info": "Couleur du texte au survol et au clic. Default marron foncé pour lisibilité sur fond blanc.",
      "default": "#332821"
    },
    {
      "type": "range",
      "id": "hero_cta_opacity_hover",
      "label": "Opacité au survol et au clic",
      "info": "Présence du bouton rempli au survol et au clic. 1 = pleine présence, valeurs plus basses = bouton voilé.",
      "min": 0.2,
      "max": 1,
      "step": 0.1,
      "default": 1
    },
    {
      "type": "header",
      "content": "Bouton CTA du hero — taille et espacement"
    },
    {
      "type": "paragraph",
      "content": "Ces réglages pilotent la taille du texte et le padding interne du bouton hero. Permettent de retrouver un rendu compact gallery (texte qui rentre étroitement dans le bouton)."
    },
    {
      "type": "range",
      "id": "hero_cta_font_size",
      "label": "Taille du texte du bouton",
      "info": "Taille de la typographie Jost light CAPS dans le bouton. Default 12 = compact gallery, valeurs plus hautes = bouton plus présent.",
      "min": 10,
      "max": 18,
      "step": 1,
      "unit": "px",
      "default": 12
    },
    {
      "type": "range",
      "id": "hero_cta_padding_horizontal",
      "label": "Espacement horizontal interne",
      "info": "Espace à gauche et à droite du texte dans le bouton. Default 32 = bouton compact.",
      "min": 16,
      "max": 56,
      "step": 4,
      "unit": "px",
      "default": 32
    },
    {
      "type": "range",
      "id": "hero_cta_padding_vertical",
      "label": "Espacement vertical interne",
      "info": "Espace au-dessus et en dessous du texte dans le bouton. Default 14 = bouton compact.",
      "min": 8,
      "max": 28,
      "step": 2,
      "unit": "px",
      "default": 14
    },
    {
      "type": "header",
      "content": "Format du hero (aspect-ratio)"
    },
    {
      "type": "select",
      "id": "hero_aspect_ratio",
      "label": "Format du hero",
      "info": "Ratio largeur/hauteur de l'image hero. 16:9 = standard cinéma (recommandé, équivalent Invisible Collection). 21:9 = panoramique large. 2:1 = large. 3:1 = bande très panoramique.",
      "options": [
        { "value": "16/9", "label": "16:9 (cinéma — recommandé)" },
        { "value": "2/1", "label": "2:1 (large)" },
        { "value": "21/9", "label": "21:9 (panoramique)" },
        { "value": "5/2", "label": "5:2 (ultra-panoramique)" },
        { "value": "3/1", "label": "3:1 (très panoramique)" }
      ],
      "default": "16/9"
    },
    {
      "type": "range",
      "id": "hero_max_height_vh",
      "min": 50,
      "max": 100,
      "step": 5,
      "unit": "vh",
      "label": "Hauteur maximale du hero (% écran)",
      "info": "Limite haute pour éviter un hero trop grand sur écrans très larges. 90 par défaut.",
      "default": 90
    },
    {
      "type": "header",
      "content": "Voile sur l'image"
    },
    {
      "type": "color",
      "id": "hero_overlay_color",
      "label": "Couleur du voile sur l'image",
      "info": "Voile très discret qui assombrit légèrement l'image pour faire ressortir le texte. Default noir.",
      "default": "#000000"
    },
    {
      "type": "range",
      "id": "hero_overlay_opacity_pct",
      "min": 0,
      "max": 50,
      "step": 1,
      "unit": "%",
      "label": "Intensité du voile",
      "info": "0 = aucun voile (image brute). 50 = voile très marqué. Recommandé : 5 à 15 (= 0.05–0.15) pour un effet imperceptible mais lisibilité accrue.",
      "default": 8
    },
    {
      "type": "header",
      "content": "Rythme titre / sous-titre / CTA"
    },
    {
      "type": "range",
      "id": "hero_spacing_title_to_subtitle",
      "min": 8,
      "max": 48,
      "step": 2,
      "unit": "px",
      "label": "Espace entre le titre et le sous-titre",
      "info": "Air vertical entre le grand titre et la ligne descriptive en dessous.",
      "default": 24
    },
    {
      "type": "range",
      "id": "hero_spacing_subtitle_to_cta",
      "min": 8,
      "max": 48,
      "step": 2,
      "unit": "px",
      "label": "Espace entre le sous-titre et le bouton",
      "info": "Air vertical entre la ligne descriptive et le bouton DÉCOUVRIR LA SÉLECTION.",
      "default": 24
    },
    {
      "type": "header",
      "content": "Indicateur de scroll"
    },
    {
      "type": "checkbox",
      "id": "hero_scroll_indicator_enabled",
      "label": "Afficher l'indicateur de scroll",
      "default": true
    },
    {
      "type": "color",
      "id": "hero_scroll_indicator_color",
      "label": "Couleur de l'indicateur",
      "info": "À aligner sur la couleur du texte du hero pour cohérence.",
      "default": "#FFFFFF"
    },
    {
      "type": "range",
      "id": "hero_scroll_indicator_opacity",
      "min": 0.2,
      "max": 1,
      "step": 0.1,
      "label": "Présence de l'indicateur",
      "info": "Discrétion de l'indicateur. 1 = pleine présence, valeurs plus basses = plus discret.",
      "default": 1
    },
    {
      "type": "range",
      "id": "hero_scroll_indicator_height",
      "min": 20,
      "max": 80,
      "step": 5,
      "unit": "px",
      "label": "Hauteur de l'indicateur de scroll",
      "info": "Longueur visuelle du trait vertical sous le hero. Plus la valeur est haute, plus l'indicateur est visible.",
      "default": 50
    },
    {
      "type": "header",
      "content": "Animation lente (Ken Burns)"
    },
    {
      "type": "checkbox",
      "id": "hero_kenburns_enabled",
      "label": "Activer l'animation lente de l'image hero",
      "info": "Effet de zoom très discret qui donne une respiration cinématographique. Désactivé automatiquement si l'utilisateur a activé la réduction de mouvement.",
      "default": true
    },
    {
      "type": "range",
      "id": "hero_kenburns_duration",
      "min": 10,
      "max": 40,
      "step": 5,
      "unit": "s",
      "label": "Durée du cycle d'animation",
      "info": "Temps pour un cycle zoom complet (aller-retour). Plus long = plus discret.",
      "default": 20
    },
    {
      "type": "range",
      "id": "hero_kenburns_amplitude_pct",
      "min": 0,
      "max": 10,
      "step": 1,
      "unit": "%",
      "label": "Amplitude du zoom",
      "info": "Zoom maximal en pourcentage. 0 = pas de zoom, 5 = zoom de 5%, 10 = zoom de 10%. Recommandé : 4 à 6 pour un effet imperceptible.",
      "default": 5
    },
    {
      "type": "select",
      "id": "image_height",
      "options": [
        {
          "value": "adapt",
          "label": "t:sections.image-banner.settings.image_height.options__1.label"
        },
        {
          "value": "small",
          "label": "t:sections.image-banner.settings.image_height.options__2.label"
        },
        {
          "value": "medium",
          "label": "t:sections.image-banner.settings.image_height.options__3.label"
        },
        {
          "value": "large",
          "label": "t:sections.image-banner.settings.image_height.options__4.label"
        }
      ],
      "default": "medium",
      "label": "t:sections.image-banner.settings.image_height.label"
    },
    {
      "type": "range",
      "id": "image_height_desktop_custom",
      "min": 30,
      "max": 80,
      "step": 1,
      "unit": "rem",
      "label": "Hauteur desktop personnalisée",
      "info": "Surcharge la hauteur du sélecteur ci-dessus sur desktop uniquement (≥750px). 50rem ≈ format panoramique large. Mobile non impacté.",
      "default": 50
    },
    {
      "type": "select",
      "id": "image_behavior",
      "options": [
        {
          "value": "none",
          "label": "t:sections.all.animation.image_behavior.options__1.label"
        },
        {
          "value": "ambient",
          "label": "t:sections.all.animation.image_behavior.options__2.label"
        },
        {
          "value": "fixed",
          "label": "t:sections.all.animation.image_behavior.options__3.label"
        },
        {
          "value": "zoom-in",
          "label": "t:sections.all.animation.image_behavior.options__4.label"
        }
      ],
      "default": "none",
      "label": "t:sections.all.animation.image_behavior.label"
    },
    {
      "type": "header",
      "content": "t:sections.image-banner.settings.content.content"
    },
    {
      "type": "select",
      "id": "desktop_content_position",
      "options": [
        {
          "value": "top-left",
          "label": "t:sections.image-banner.settings.desktop_content_position.options__1.label"
        },
        {
          "value": "top-center",
          "label": "t:sections.image-banner.settings.desktop_content_position.options__2.label"
        },
        {
          "value": "top-right",
          "label": "t:sections.image-banner.settings.desktop_content_position.options__3.label"
        },
        {
          "value": "middle-left",
          "label": "t:sections.image-banner.settings.desktop_content_position.options__4.label"
        },
        {
          "value": "middle-center",
          "label": "t:sections.image-banner.settings.desktop_content_position.options__5.label"
        },
        {
          "value": "middle-right",
          "label": "t:sections.image-banner.settings.desktop_content_position.options__6.label"
        },
        {
          "value": "bottom-left",
          "label": "t:sections.image-banner.settings.desktop_content_position.options__7.label"
        },
        {
          "value": "bottom-center",
          "label": "t:sections.image-banner.settings.desktop_content_position.options__8.label"
        },
        {
          "value": "bottom-right",
          "label": "t:sections.image-banner.settings.desktop_content_position.options__9.label"
        }
      ],
      "default": "middle-center",
      "label": "t:sections.image-banner.settings.desktop_content_position.label"
    },
    {
      "type": "select",
      "id": "desktop_content_alignment",
      "options": [
        {
          "value": "left",
          "label": "t:sections.image-banner.settings.desktop_content_alignment.options__1.label"
        },
        {
          "value": "center",
          "label": "t:sections.image-banner.settings.desktop_content_alignment.options__2.label"
        },
        {
          "value": "right",
          "label": "t:sections.image-banner.settings.desktop_content_alignment.options__3.label"
        }
      ],
      "default": "center",
      "label": "t:sections.image-banner.settings.desktop_content_alignment.label"
    },
    {
      "type": "checkbox",
      "id": "show_text_box",
      "default": true,
      "label": "t:sections.image-banner.settings.show_text_box.label"
    },
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "t:sections.all.colors.label",
      "default": "scheme-1"
    },
    {
      "type": "header",
      "content": "t:sections.image-banner.settings.mobile.content"
    },
    {
      "type": "checkbox",
      "id": "stack_images_on_mobile",
      "default": true,
      "label": "t:sections.image-banner.settings.stack_images_on_mobile.label"
    },    
    {
      "type": "select",
      "id": "mobile_content_alignment",
      "options": [
        {
          "value": "left",
          "label": "t:sections.image-banner.settings.mobile_content_alignment.options__1.label"
        },
        {
          "value": "center",
          "label": "t:sections.image-banner.settings.mobile_content_alignment.options__2.label"
        },
        {
          "value": "right",
          "label": "t:sections.image-banner.settings.mobile_content_alignment.options__3.label"
        }
      ],
      "default": "center",
      "label": "t:sections.image-banner.settings.mobile_content_alignment.label"
    },
    {
      "type": "checkbox",
      "id": "show_text_below",
      "default": true,
      "label": "t:sections.image-banner.settings.show_text_below.label"
    }
  ],
  "blocks": [
    {
      "type": "heading",
      "name": "t:sections.image-banner.blocks.heading.name",
      "limit": 1,
      "settings": [
        {
          "type": "inline_richtext",
          "id": "heading",
          "default": "t:sections.image-banner.blocks.heading.settings.heading.default",
          "label": "t:sections.image-banner.blocks.heading.settings.heading.label"
        },
        {
          "type": "select",
          "id": "heading_size",
          "options": [
            {
              "value": "h2",
              "label": "t:sections.all.heading_size.options__1.label"
            },
            {
              "value": "h1",
              "label": "t:sections.all.heading_size.options__2.label"
            },
            {
              "value": "h0",
              "label": "t:sections.all.heading_size.options__3.label"
            },
            {
              "value": "hxl",
              "label": "t:sections.all.heading_size.options__4.label"
            },
            {
              "value": "hxxl",
              "label": "t:sections.all.heading_size.options__5.label"
            }
          ],
          "default": "h1",
          "label": "t:sections.all.heading_size.label"
        }
      ]
    },
    {
      "type": "text",
      "name": "t:sections.image-banner.blocks.text.name",
      "limit": 1,
      "settings": [
        {
          "type": "inline_richtext",
          "id": "text",
          "default": "t:sections.image-banner.blocks.text.settings.text.default",
          "label": "t:sections.image-banner.blocks.text.settings.text.label"
        },
        {
          "type": "select",
          "id": "text_style",
          "options": [
            {
              "value": "body",
              "label": "t:sections.image-banner.blocks.text.settings.text_style.options__1.label"
            },
            {
              "value": "subtitle",
              "label": "t:sections.image-banner.blocks.text.settings.text_style.options__2.label"
            },
            {
              "value": "caption-with-letter-spacing",
              "label": "t:sections.image-banner.blocks.text.settings.text_style.options__3.label"
            }
          ],
          "default": "body",
          "label": "t:sections.image-banner.blocks.text.settings.text_style.label"
        }
      ]
    },
    {
      "type": "buttons",
      "name": "t:sections.image-banner.blocks.buttons.name",
      "limit": 1,
      "settings": [
        {
        "type": "header",
        "content": "t:sections.image-banner.blocks.buttons.settings.header_1.content"
        },
        {
          "type": "text",
          "id": "button_label_1",
          "default": "t:sections.image-banner.blocks.buttons.settings.button_label_1.default",
          "label": "t:sections.image-banner.blocks.buttons.settings.button_label_1.label",
          "info": "t:sections.image-banner.blocks.buttons.settings.button_label_1.info"
        },
        {
          "type": "url",
          "id": "button_link_1",
          "label": "t:sections.image-banner.blocks.buttons.settings.button_link_1.label"
        },
        {
          "type": "checkbox",
          "id": "button_style_secondary_1",
          "default": false,
          "label": "t:sections.image-banner.blocks.buttons.settings.button_style_secondary_1.label"
        },
        {
        "type": "header",
        "content": "t:sections.image-banner.blocks.buttons.settings.header_2.content"
        },
        {
          "type": "text",
          "id": "button_label_2",
          "default": "t:sections.image-banner.blocks.buttons.settings.button_label_2.default",
          "label": "t:sections.image-banner.blocks.buttons.settings.button_label_2.label",
          "info": "t:sections.image-banner.blocks.buttons.settings.button_label_2.info"
        },
        {
          "type": "url",
          "id": "button_link_2",
          "label": "t:sections.image-banner.blocks.buttons.settings.button_link_2.label"
        },
        {
          "type": "checkbox",
          "id": "button_style_secondary_2",
          "default": false,
          "label": "t:sections.image-banner.blocks.buttons.settings.button_style_secondary_2.label"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "t:sections.image-banner.presets.name",
      "blocks": [
        {
          "type": "heading"
        },
        {
          "type": "text"
        },
        {
          "type": "buttons"
        }
      ]
    }
  ]
}
{% endschema %}
